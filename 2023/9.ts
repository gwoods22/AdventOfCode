import { strData } from './load.js';

const startTime = performance.now()


console.log('Part A is', );
console.log('Part B is', );

console.log('Took:', ((performance.now() - startTime)/1000).toFixed(4), 'seconds to run');